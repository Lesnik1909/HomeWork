using System;

namespace MyHomeWork
{
    class Program
    {
        static void DrawBar(int value, int maxValue, ConsoleColor color, int position)
        {
            int persent;
            persent = value * 100 / maxValue;
            ConsoleColor defaultColor = Console.BackgroundColor;
            string bar = "";
            if ((persent % 10) < 5)
            {
                persent /= 10;
            }
            else
            {
                persent = persent / 10 + 1;
            }
            for (int i = 0; i < persent; i++)
            {
                bar += '#';
            }
            Console.SetCursorPosition(0, position);
            Console.Write('[');
            Console.BackgroundColor = color;
            Console.Write(bar);
            Console.BackgroundColor = ConsoleColor.Red;
            bar = "";
            for (int i = persent; i < 10; i++)
            {
                bar += '_';
            }
            Console.Write(bar);
            Console.BackgroundColor = defaultColor;
            Console.Write("]");
        }
        static void Main(string[] args)
        {
            int health = 240, maxHelth = 275;
            int mana = 128, maxMana = 350;
            DrawBar(health, maxHelth, ConsoleColor.Green, 0);
            DrawBar(mana, maxMana, ConsoleColor.Blue, 1);
        }
    }
}
